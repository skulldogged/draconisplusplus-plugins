# JSON Format Plugin
# Cross-platform output formatter with compact and pretty-print modes

set_variable('plugin_name', 'json_format')
set_variable('plugin_sources', files('json_format.cpp'))
set_variable('plugin_deps', [])
set_variable('plugin_platform', 'all')