# YAML Format Plugin
# Cross-platform output formatter for YAML output using RapidYAML (single-header)

set_variable('plugin_name', 'yaml_format')
set_variable('plugin_sources', files('yaml_format.cpp'))
set_variable('plugin_deps', [])
set_variable('plugin_platform', 'all')